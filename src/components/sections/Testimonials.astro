---
import Card from "../Card.astro"
import { users } from "../../constans/index";
import NavLink from "../NavLink.astro";
import SwipperUsers from "../SwipperUsers";
---

<section>
  <h2 class="text-center text-3xl my-9 text-darkBlue font-bold">
    What they’ve said
  </h2>
  <section
    class="cards-mobile flex flex-col items-center justify-center gap-14 md:hidden md:items-center md:gap-4 pt-6"
  >
  <SwipperUsers client:load />
</section>

<section class="cards-desktop hidden md:flex md:justify-between md:items-center md:gap-3 p-6">
  { users.map(user => (
    <Card
      username={user.usename}
      testimonial={user.testimonial}
      imageUrl={user.imageURL}
    />
  ))}
  
  </section>
  <div class="flex items-center justify-center mb-6 mt-3">
    <NavLink
      bg="bg-brightRed hover:bg-brightRedLight"
      color="text-white"
      text="Get Started"
    />
  </div>
</section>
